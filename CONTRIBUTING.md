# Contributing

**简体中文** | [English](CONTRIBUTING.en.md)

欢迎提交风格配方、复现结果与缺陷修复。请先确认修改对应的使用场景。

风格数据位于 `wireframes/styles.json`；网页与 CLI 渲染入口为 `scripts/koi.py`。新增配方需提供稳定 ID、名称、来源与许可、完整实际提示词、样图和验证状态。不得把未经许可的第三方图片或整套提示词搬入仓库。

样图须注明 AI 生成、输入主题与实际请求。单张效果、用户方向认可、跨主题验证和跨模型验证分别记录；不要把一个通过的样例描述成普遍保证。修改已有配方时更新配方版本，并说明与原样图的差异。

提交前运行：

```sh
python3 -m unittest discover -s tests -v
python3 scripts/serve.py
```

在浏览器验证受影响的操作。请勿提交密钥、个人主题、缓存或运行日志。当前接口仅用于本地体验，不作为公开托管服务。

纯文档修改按需核对示例和链接。面向用户的中英文文档保持同步，指向尚未翻译资料的链接应标明语言。
