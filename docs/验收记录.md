# 原型验收记录

> 当前代码已于 2026-09-09 公开至 koi-lee/koi-stylekit，首个提交 3640f71。下文各阶段的“未提交/未发布”为当时状态，非当前状态。正式 Release 与社交推广仍未发布。


日期：2026-09-09。范围：本地单文件流程原型，非产品完成或视觉效果验收。

## 浏览器已检查

- 搜索无结果时出现空状态，清除后恢复 3 个风格。
- 勾选两个风格后比较可用，表格展示构成、用途及注意事项。
- 选择墨线点色后，内容表单正确展示该风格。
- 空主题提交有就地错误提示并聚焦输入框。
- 示例主题和独立标题正确进入预览；未指定比例时不拼入比例。
- 复制按钮返回“提示词已复制”，没有独立读取系统剪贴板核对。
- 下载按钮已触发，页面准确提示去下载列表确认；未取得落盘文件及解析证据。
- 开始下一主题会清空主题和标题、沿用风格。
- 记住风格后刷新出现沿用入口，进入表单不恢复主题。
- 已观察窄窗口页面布局；未覆盖完整设备和浏览器矩阵。

## 待验收与后续

- 用户确认页面结构、字段与复用流程。
- 实际样图、六种风格质量、生图一致性均未验证。
- 正式 Skill、Python CLI、稳定数据格式、中英提示词和模型适配未实现。
- GitHub 仓库、许可证、开源发布、平台文案与视频均未完成。
- 产品矩阵登记及主站入口待正式范围与发布入口确定后处理，不修改其他项目。

当前文件：README.md、AGENTS.md、.gitignore、wireframes/index.html、docs/项目流程图.md、docs/验收记录.md。全部未提交。

## v0.2 本地统一渲染

已实现：网页与 CLI 调用 scripts/koi.py 的同一渲染入口，统一 JSON，独立标题与比例，配色选择、未验证变体标识。启动改为 scripts/serve.py，回环接口只接受同源 JSON，不记录正文。

自动验证：5 项 unittest，包含真实临时 HTTP 端口与 CLI 的三种配色策略结果全字段相等、非法输入、独立标题、正文按字面保留。

浏览器验证：双色孔版 → 示例绿色书本 → 颜色提示阻止导出 → 保留主题颜色 → 成功预览，固定蓝红限制已移除，未验证变体提示可见。服务中断时已验证主题与选择保留、预览按钮恢复；将网络错误改为中文恢复提示。下载文件落盘本轮尚未实测。

未完成：广泛配色语义识别、配色变体生图验证、跨环境安装验收、GitHub 发布。

## 独立目录安装验收

复制到新临时目录（不含 .git 与缓存）后，5 项测试通过；从 /tmp 通过绝对脚本路径运行 CLI 成功；以不同端口启动服务，首页、styles.json、三张画廊图片 HTTP 检查通过。Skill 格式校验通过。Python 环境为本机现有环境，无第三方依赖安装。尚未验证 Windows/Linux、最低 Python 版本及宿主自动发现 Skill。

新增 MIT、ATTRIBUTION.md、CONTRIBUTING.md 与开源发布草稿，尚未提交、推送或发布。社交文案为通用草稿，目标平台规则审核未完成。

## 公开入口整理（本地待提交）

README 增加候选样图、下载/克隆/启动/命令行说明；历史未发布状态注明阶段日期。风格 ID 更正为 emotional-sketch、colored-pencil-diary；旧 ID 在 CLI/API 和浏览器保存记录中保留兼容。

6 项本地测试与独立目录重复安装通过，包含旧 ID 兼容及 HTTP/CLI 一致性。浏览器彩铅入口 → 示例 → 预览正常。README 本地链接与图片路径检查通过。新增 GitHub CI 矩阵（三种系统 × Python 3.10/3.14），尚未推送，因此不声明远程运行成功。实际产品案例、正式 Release 与社交推广仍未完成。

## 718e17c 远程与安装验收

已推送 718e17c，GitHub Actions 34353689951 的 Windows/macOS/Ubuntu × Python 3.10/3.14 六个任务全部成功。运行地址：https://github.com/koi-lee/koi-stylekit/actions/runs/34353689951 。

使用官方 skill-installer 从固定提交安装完整仓库至 Codex Skill 目录；安装后的脚本在独立工作目录成功生成提示词，Skill 格式校验通过。宿主下一轮自动发现仍待确认。

浏览器实际点击下载，Downloads 中 koi-stylekit.json 已落盘；以“下载验收：猫在雨中撑伞”为输入，解析后的所有 JSON 字段与 CLI 输出相等。

## 纸雕 Alpha 收尾

当前本地目录含四种风格。纸雕中文导出配方完成天气、书店两主题无参考图实测，用户反馈“差不多吧”，接受当前视觉基线。额外元素、背景与布局约束、跨模型及重复生成稳定性不视为通过。实际风格包保留调用时元数据，不修改历史证据。已安装 Skill 仍是旧提交，最新新增案例尚未提交、推送或发布。宿主已显示 koi-stylekit 技能，自动发现得到确认。

## 2026-09-10：300 项候选目录

- 范围：40 个家族与 260 个画法变体，300 张样图，新增 296 份真实生成输入；中英文 README、贡献指南与扩充说明。
- 10 项自动测试通过；覆盖 1,264 个用途/配色组合，300 项真实 CLI 与 632 次 HTTP 导出已验证一致。
- 300 PNG 完整性与文件无重复检查通过；596 个图片/JSON HTTP 路径与本地内容一致。
- 浏览器验证搜索、家族筛选、分页、空结果恢复、三项比较、详情和主题预览；最终确认 300 张、第 13 页、第 300 项详情。
- 视觉状态：新增样图均为同主题单次候选，尚无逐项用户验收与跨主题稳定性证据。部分变体差异小，背景或媒介约束有偏离。
- 按用户“测试通过后推送”授权交付候选目录；本记录不代表正式版本发布或全量视觉通过。

## 2026-09-10 家族、收藏与新候选

- 默认 48 家族，12 精选起点；新风格实验 8 项；全部配方保留 308 项。
- 浏览器验证收藏后 1 项、刷新仍为 1 项、取消后 0 项；精选 12 项；纸雕家族展开 8 项。保留顶部整体吸附。
- 8 张内置 imagegen 原始 PNG 与完整输入 JSON 已保存到 wireframes/assets/catalogue，浏览器逐行查看样图。纸雕灯箱、立体书增加了植物等环境元素；玻璃样图背景较满；双色抖动不宣称像素级双色准确。均等待用户视觉验收。
- 10 项自动测试通过，目录断言按新增 8 项更新为 308，覆盖全目录导出、PNG 完整性与唯一性、输入配方一致性。git diff --check 通过。
- 尚未提交或推送；精选效果确定后再补跨主题样图和用途预览。

## 2026-09-10 用途与手机检查

- 新增 cases/style-applications/：纸雕灯箱、立体书、气泡马赛克各生成三张人物/耳机/知识场景样图，共 9 PNG 与 9 份完整输入 JSON。配方保持原样，使用内置 imagegen，无参考图。封面和功能卡片为 HTML 排版示意，未修改其他产品。
- 观察：立体书人物成为真人持书，产品未从书中弹起；灯箱保留光影但增加环境、耳机偏写实；马赛克产品混入立体质感。页面与目录风险已标注，不声明跨主题通过。
- 浏览器视口 390×844、320×568：手机顶部从约 374px 收紧到约 220px；说明和高级筛选可展开，展开区有高度上限且可滚动，隐藏条件以计数提示。纸艺筛选返回 8 基础家族，收起后显示 1 项高级条件。桌面继续整体吸顶。已恢复浏览器原视口；非真机证据。
- 11 项测试通过，新增检查 9 份输入与共享渲染器一致、图片和预览链接存在。视觉质量仍等待用户验收；未提交、推送。

## 2026-09-10 每页展示数量

- 默认 50 条；顶部与底部同步支持 50、100、150、200 和全部。修改数量回到第一页，保留筛选；图片继续懒加载。
- 浏览器全部配方实测：50/100/150/200/全部分别渲染 50/100/150/200/308 张卡片，对应 7/4/3/2/1 页。第二页修改底部数量回到第一页；零结果为 1/1 页且下一页禁用，清除筛选恢复 50 条。
- 未提交、推送。

## 2026-09-10 SEO 与 AI 检索入口

- 画廊页面增加中文标题、描述、关键词、Open Graph、Twitter Card 与 `SoftwareApplication` JSON-LD。
- 新增 `wireframes/robots.txt` 与 `wireframes/llms.txt`，说明项目用途、关键文件、限制与机器可读入口。
- README 中英文版本均链接上述入口；正式部署后仍需把 robots 中的 Sitemap 占位说明替换为正式域名，并向搜索引擎提交站点。
- 本地 HTTP 检查确认元数据、JSON-LD、robots 与 llms.txt 均可访问；尚未取得正式域名索引或搜索排名证据。

## 2026-09-10 纯静态网页版（本地，未提交）

用户授权静态迁移，沿用已认可界面。网页不再请求 /api/render；Python CLI/Skill 保留，共用目录与规则 JSON。12 项测试通过，7,797 请求跨运行时一致，含 Unicode 回归。普通静态服务器的根路径及子路径完整流程通过；实际剪贴板和下载 JSON 与 CLI 一致；320/390/440 手机宽度检查，资源加载失败提示通过。约 67.44 MB 静态包保留原始素材另存，缩略图平均 35.5 KB。

本轮未提交、推送、部署、修改主站或确认真机 Safari。完整命令、产物路径、验证范围和接入事项见 [静态版交接](static-web.md)。

## 静态版提交收尾

用户确认主站本地已同步。收藏与家族按钮增加 10px 间距，距主按钮 12px；桌面、390px 和 320px 检查通过。收藏仅保存当前 origin 的风格 ID，不跨设备或域名同步，页面已明确说明。2026-09-10 用户授权继续提交收尾；此前“未提交”段落是阶段记录。主站公网部署、正式地址和 iPhone Safari 真机仍由主站会话验收，不以本地同步代替上线。

## 2026-09-11 正式 HTTPS 画廊独立验收

- 正式入口：https://www.starshoreai.com/koi-stylekit/gallery/ 。响应 HTTP 200。
- 隔离 Chrome 浏览器验证 1280px 桌面、320/390/440px 触屏手机模拟：搜索、筛选、100 条分页、比较、收藏刷新保留、详情、主题输入、配色冲突处理、预览、复制和 JSON 下载通过。
- 实际下载 JSON 与同输入 Python CLI 逐字段一致；本轮为关键场景线上复验，全目录 7,797 组对照属于此前本地验证。
- 无横向溢出，按钮间距正常；代表图片解码成功。模拟缩略图、目录、规则加载失败时提示正常；规则失败保留主题输入。
- 流程未观察到非 GET 或 /api/ 请求，无页面未捕获异常。复制测试已授予剪贴板权限，未验证默认拒绝权限场景。
- 本机证据在忽略目录 outputs/live/，包含截图、下载文件及 browser-report.json，不随 Git 发布。
- 未覆盖真实 iPhone Safari、真实移动网络性能、全部线上资源逐字节校验或跨主题生图质量。此轮不修改主站、不部署。

## 2026-09-11 发布收尾与发现性检查

- 文档提交 0fd485ce34c1d621820751f80974b927a3406e36 的 GitHub Actions 34550941738 已 completed/success。
- 主站 robots.txt 为 Allow: /，声明正式 sitemap.xml；sitemap 包含 /koi-stylekit 与 /koi-stylekit/gallery/。两页 HTTP 200、canonical 正确，未发现 meta noindex 或 X-Robots-Tag。
- 一次公开 site: 搜索未返回结果，不等于未收录；尚未取得 Search Console / Bing Webmaster 索引报告。
- 主站根 /llms.txt 为 404；画廊 /koi-stylekit/gallery/llms.txt 为 200。根文件缺失不作为搜索索引阻断或 AI 收录失败证据。
- 本地修正画廊 og:description，将“300+ 种风格”改为 308 项配方、48 家族、260 变体；需要后续构建并由主站同步才能在线生效。

## 2026-09-11 纸雕人物与物品单次实验

沿用 layered-paper CLI 导出，仅替换主题，single / 16:9 / style，无参考图，内置 imagegen 各一次。原始 PNG 与 JSON 保存在 cases/style-applications/paper-person 和 paper-object。人物配色有额外颜色，耳机上边距偏小；均未获用户视觉验收，不声明重复生成或跨模型通过。已加入 paper-kit.html 案例页，历史证据保持不变。

## 纸雕案例收尾验收

用户认可新增人物与耳机视觉方向；保留配色与上边距偏差说明，不提升为稳定配方。完整 dist 构建通过。1280px 四案例实际剪贴板内容与提示词一致；320/390/440px 四案例复制成功提示、真实 JSON 下载与文本相等、拒绝复制时选中文本回退均通过。四尺寸无横向溢出、无 pageerror。图片均解码成功；手机自动化为避免等待屏外懒加载而将测试页图片设为 eager，不代表验证滚动懒加载触发。无真机证据。

原 4333 服务无响应，预览使用 http://127.0.0.1:4334/cases/style-applications/paper-kit.html 。截图保存在 outputs/live/paper-verified-*.png。未部署主站。

## 纸雕试用说明与持续回归

补充案例用途选择、仅替换主题的操作说明、JSON 不是通用生图预设及常见偏差处理建议；建议明确标为未系统实测。新增四份保存输入、页面提示词与当前渲染器的防漂移测试。13 项测试通过，7,797 请求跨运行时一致。新增可移植 scripts/verify_paper.cjs，四视口复制提示、实际下载内容、拒绝复制回退、滚动懒加载与无溢出检查通过。未修改配方或新增模型调用；无真机及真实用户采用证据。
