# AI 面试卡片配图候选

当前结论：用户在天气与书店实测后反馈“差不多吧”，接受当前纸雕为 Alpha 视觉基线。仍保留额外元素与布局漂移限制；产品接入、学习效果、重复生成及跨模型稳定性未验收。以下按时间保留实验记录，“待验收”是当时状态。

场景依据：现有产品的“先看懂”通俗理解层需要降低抽象概念理解门槛。本轮只在 Koi StyleKit 内制作候选，不修改目标产品或题库，不使用用户回答和账户信息。

入口：wireframes/cases/interview-cards/index.html。两张图与对应实际 JSON 请求在同一目录。

使用已安装 koi-stylekit Skill（宿主本轮已显示在可用技能列表）→ 安装目录 CLI 输出风格包 → 原样 prompt_zh 传内置 imagegen。两次均为 duotone-print、single、16:9、style 配色策略，无参考图，未手工改写风格段落。

观察：红蓝及颗粒感延续；第一张单场景，第二张自行扩为动作序列并出现符号状笔迹，人物外形不同。结果不证明角色或版式稳定，也不证明教学有效。

状态：最初人物插画未满足用户期望，反馈为偏学习场景。追加技术线稿、几何与纸雕候选后，用户明确偏好纸雕效果。

## 纸雕系列（2026-09-09）

- 预览：wireframes/cases/interview-cards/paper-series.html。
- 第一张 paper.png：检索资料类比，用户选为风格方向。
- 第二张 paper-tool.png：任务卡 → 计算器 → 结果卡。内置 imagegen 一次调用，使用同目录 paper.png 作为风格参考；完整实际输入保留在 paper-tool-prompt.txt。
- 纸雕提示词为自行编写，没有标为两个上游仓库的复现，当时尚未加入核心风格目录（后续已接入）。
- 第二张可观察到米白纤维纸底、青绿主体、珊瑚重点及纸张浅浮雕延续；仅两张样例，不能证明跨主题稳定复现。工具图是概念类比，不是完整调用协议。
- 系列一致性待用户视觉验收，目标产品接入与学习效果验证未完成。没有发布案例、Release 或社交内容。

### 天气查询修订

用户指出计算器容易把工具调用理解得过窄，已同意改用天气查询场景。新图 paper-weather.png 使用 paper.png 作风格参考，内置 imagegen 一次生成，实际提示词保留在 paper-weather-prompt.txt。预览页以天气案例为主，计算器保留在早期案例折叠入口。图外说明用户提问、AI 请求工具、应用执行并返回、AI 根据数据回答；天气为示意，未调用真实天气接口。新图待用户视觉验收，未提交或发布。

### 通用配方接入

新增 layered-paper 到共享风格目录，画廊与 CLI 可导出。中文通用配方由英文样例提炼，尚未直接生图验证；样图不是该导出配方的精确生成证据。现有 6 项测试通过，纸雕 CLI 与运行中 HTTP 输出逐字段一致。尚未更新已安装 Skill 副本，未提交或推送。

### 中文导出直接生图实测

paper-direct.json 为实际 CLI 导出包，prompt_zh 原样用于内置 imagegen，一次调用、无参考图，结果为 paper-direct.png。纸张层次与主配色延续；新增地图与机器人，箭头及雨滴配色发生变化。与英文加参考图候选不是单变量实验，不对差异作因果归因。用户视觉验收、跨主题复用仍待验证。原导出包保留当时验证状态，不回写历史证据。

### 第二主题：书店取书

paper-bookshop.json 的 prompt_zh 原样用于内置 imagegen，一次调用、无参考图，仅替换主题，风格配方不变。结果 paper-bookshop.png 保留纸张质感、主配色与可辨认的递书动作；额外生成柜台与植物，未完全遵守背景约束。两主题观察不等于稳定性验收，用户视觉验收仍待完成。
