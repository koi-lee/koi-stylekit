# 纯静态网页版交接

## 当前状态（2026-09-11）

[正式画廊](https://www.starshoreai.com/koi-stylekit/gallery/)已通过 HTTPS 浏览器验收。1280px 桌面及 320/390/440px 手机模拟流程通过，详见 [验收记录](验收记录.md)。以下构建与本地验证记录保留原执行日期；其中“本轮未部署”描述的是 9 月 10 日静态迁移阶段。

## 本次约定

访客通过网页完成浏览 → 选风格 → 填主题 → 配色选择 → 预览 → 复制/下载，无需安装 Python。沿用现有流程，不新增账户、遥测、模型调用或上传；主题只留在当前浏览器内存。收藏仍只保存风格 ID。样图来自历史候选生成，不代表跨主题稳定。

## 给星岸主站会话

构建产物：本项目 `dist/`，完整上传其内容即可。支持域名根目录和子目录（例如 `/koi-stylekit/gallery/`）；目录 URL 须带尾斜杠或由静态服务器重定向。所有运行资源都是相对地址，无 `/api/render` 请求、无 Python 服务依赖、无第三方运行时 CDN。

不要发布仓库根目录、测试输出或 Python 预览服务器。保留 `.nojekyll`，GitHub Pages 可发布整个构建产物。本轮未配置 Pages、域名、canonical 或站点提交；不要把本地地址当在线体验链接。

```sh
python3 -m pip install -r requirements-build.txt
python3 scripts/build_static.py
python3 -m http.server 4321 --bind 127.0.0.1 --directory dist
```

构建依赖 Pillow 11.x；使用者无需依赖。`dist/` 与 `outputs/` 已忽略，不提交生成资源。原始 PNG/输入 JSON 保留在 `wireframes/`。构建生成 WebP 缩略图（最大 480px）和详情图（最大 1200px）；列表懒加载缩略图，详情按需载入大图。构建脚本或源图更新会使对应图片缓存失效。发布前建议从干净的构建目录生成，避免目录删除后留下旧产物。

当前产物约 **67.44 MB（64.32 MiB）**；目录原始图片共 **772.82 MB**。308 张缩略图共 **10.94 MB**，平均约 **35.5 KB**；详情图共 **54.78 MB**。这是完整发布包，非首屏下载量。用途案例的九张图片也已压缩，原图不进入发布包。

## 行为一致性与维护

`wireframes/styles.json` 和 `render-rules.json` 是两端共享数据，Python `scripts/koi.py` 为行为基准；浏览器实现为 `renderer.mjs`。保留 CLI、旧风格 ID 和旧 HTTP 测试。当前未自动翻译 Python 代码，仍有两份流程实现，必须通过对照测试防止漂移。

```sh
# 除 Python 外，对照测试要求 Node.js 22+
python3 -m unittest discover -s tests -v
```

12 项测试通过。7,797 组请求逐字段对照，覆盖 308 个风格的全部支持用途 × 三种配色策略 × 四种比例，以及非法输入、旧 ID、emoji、占位符字面量和 Unicode 空白。规则和配方文字只在共享数据中维护。CI 已添加 Node 设置，远程 CI 本轮未运行。

## 浏览器验证

普通 `http.server`：4321 提供 `dist/` 根路径，4320 提供仓库目录，以 `/dist/` 验证子路径。自动化脚本 `scripts/verify_static.cjs` 需要 Playwright 与 Chrome；可设置 `BROWSER_CHANNEL`。在项目目录安装 Playwright 后运行 `node scripts/verify_static.cjs`；本机使用已有依赖，没有给使用者增加依赖。

验证覆盖：48 家族、100 条分页、比较、收藏刷新保留、详情、配色阻止预览、保留主题配色、剪贴板内容、实际下载文件与 CLI 全字段一致；1280px 桌面及 320/390/440px 手机宽度无横向溢出。静态根路径与子路径均加载，图片 decode 成功。网络只出现 GET，未观察到主题提交请求；浏览器无未捕获异常。

本地截图、下载文件、报告在 `outputs/static/`。这是 Chrome 自动化及截图检查，未宣称 iPhone Safari 真机验收。剪贴板在 HTTPS/localhost 且权限允许时自动复制；被拒绝时提供选中文本手动复制，不影响 JSON 下载。

## 未完成与接入注意

- 主站已接入并上线，后续部署路径与缓存策略仍由主站维护；本项目不直接修改主站。
- 真机 Safari、其他浏览器和真实移动网络性能仍待验收。
- 五语仅指项目文档，网页和提示词仍主要是中文。
- 308 项为 48 家族加 260 变体，样图仍为候选。
- 纯静态版本不等于离线 PWA；首次资源加载需要网络，资源失败提示刷新。
- 旧 Python `/api/render` 为 CLI/历史兼容保留，不属于部署产物，也不被网页调用。

## 纸雕案例包增量交接（2026-09-11）

重新构建完整 dist/ 后由主站沿现有流程同步，新增路径为 cases/style-applications/paper-kit.html，入口位于实际用途案例页。页面包含天气、书店、完整人物、独立耳机四个案例及原始 JSON；人物和耳机方向获用户认可，配色偏差、重复生成与跨模型限制继续保留。原始 PNG 留在源码，构建产物使用 WebP。

同步时同时检查画廊 og:description 的准确配方数量文案。不要仅复制 HTML：四张 WebP 和四份 JSON 也是必需资源。当前完整产物 67,603,710 字节；尚未在主站部署本增量。

## 纸雕案例专项回归

构建并用普通静态服务器运行 dist 后，在已安装 Playwright 和 Chrome 的环境执行：

```sh
GALLERY_URL=http://127.0.0.1:4335/ node scripts/verify_paper.cjs
```

GALLERY_URL 为画廊根入口，须带尾斜杠，也可使用子路径；BROWSER_CHANNEL 默认 chrome。脚本使用隔离浏览器，覆盖 1280/320/390/440px 的四案例复制成功提示、JSON 内容与显示提示词相等、拒绝复制时手动选择回退、真实滚动图片加载、无溢出与 pageerror。截图输出至忽略目录 outputs/paper-check/。不进行账号或服务器写操作，不代表真机验证。

### 用途发现入口（2026-09-11，本地）

画廊卡片前新增文章/社交封面、产品/网站配图、课程/知识讲解入口，分别映射已有 cover、single、explain；选择风格后沿用用途。点击用途入口重新开始家族浏览，清除其他筛选。入口位于列表内，不增加吸顶区高度。搜索增加用途同义词，不代表对应配图的生成质量已验证。

本地验证：13项Python测试通过，含7797项跨运行时请求；1280/390宽度浏览器验证产品/课程搜索、用途选择、表单传递及提示词预览，无横向溢出。证据位于 outputs/demand-check/discovery-*.png 和 verify-discovery.cjs。未提交、未部署，视觉待用户验收。

### 2026-09-11 SEO 与 AI 检索补充（本地，未发布）

- 已有基础：title、description、Open Graph、SoftwareApplication、robots.txt、llms.txt。
- 本次补充：HTML 内直接提供用途、流程、数量、隐私、语言和能力边界问答；不依赖脚本读取。更新描述与结构化数据，移除过时的版本声明；llms.txt 改用实际产物相对路径及 GitHub 文档链接。
- 线上只读检查：主站根 robots 允许抓取并指向 sitemap；sitemap 已包含 Koi 介绍页与画廊，尚未看到纸雕案例。域名 canonical、案例 sitemap、站长平台验证/提交状态交主站任务核对。画廊子目录 robots 不替代域名根 robots。
- Google、百度、搜狗、360 共享可抓取 HTML、清晰标题、真实内容与有效链接等基础，不需要各做一套关键词页。另检查 Bing 以覆盖更多检索入口。未获得各平台后台证据，不能声称已提交、已收录或被 AI 引用。
- llms.txt 仅作为补充资料索引，不承诺平台支持。Google 官方明确其搜索（含 AI 功能）不使用 llms.txt 提升可见度：https://developers.google.com/search/docs/fundamentals/ai-optimization-guide
- 百度官方资源平台当前入口 https://zy.baidu.com/；搜狗和360站长入口本次公开读取失败，状态仍待主站核实。不添加未经证实的提交脚本、验证 token 或第三方跟踪。
- 没有修改渲染规则；13 项测试通过，包含 7797 次浏览器/Python 一致性请求。本次和用途发现入口一起交付，未 commit、push 或部署。

### 浅色方向收敛与固定交付（2026-09-11）

用户已选择简洁浅色、插画为主角。当前源码保持中性浅色画布、原图颜色及原有筛选，不采用 outputs/theme-study 中深色与混合视觉草案；这些草案不进入 dist。本次无需额外视觉改动。用途入口位于非吸顶列表区域，SEO 问答位于底部，不挤占首屏筛选区域。主站负责其导航、sitemap 与 SEO 接入，源码项目负责固定静态包；双方不互相转派。产物与校验清单位于忽略目录 outputs/handoff，未提交、推送或部署。

### 卡片点击与分页收敛（2026-09-11，本地）

- 每页条数仅保留顶部选择器，底部只保留上一页/页码/下一页。
- 图片、标题及非交互空白点击打开已有详情；显式详情按钮保留键盘路径。整卡不增加 Tab 停靠点，不嵌套按钮。排除所有独立控件、比较 label 和验证 badge；文字选中时不打开详情。
- 只对 hover + fine pointer 添加轻微边框和阴影，无位移、缩放或发光；原有焦点轮廓保留。
- 回归入口：`GALLERY_URL=http://127.0.0.1:4339/ node scripts/verify_cards.cjs`（需 Playwright）。覆盖 1280/390 图片/标题/空白、详情按钮键盘打开与关闭、独立控件、拖选、50/100/150/200/全部及页码重置。
- 用户验收证据（由主站任务转达）：用户实际完成课程/知识讲解→选择风格→表单三步知识讲解。此项为用户反馈，不算本任务自动化或真机测试。
- 保留已有用途与 SEO 改动；未提交、推送或部署。

### 卡片层次与详情滚动（2026-09-11，后续用户反馈）

用户新的立体卡片要求取代此前“不位移”：精细指针hover轻抬3px、双层柔影，移出恢复；减少动态时无位移。原图无滤镜或放大。详情关闭按钮移至不滚动的右上header（44px以上），正文独立滚动。打开时保存body样式/列表坐标/触发控件，固定背景；关闭、Escape、使用风格统一恢复，原生dialog维持焦点约束。

`node scripts/verify_dialog.cjs` 在1280/390验证长详情滚到底、关闭控件可见、背景锁定、精确坐标与有效body样式恢复、焦点恢复、连续Enter/Escape、使用风格路径和减少动态；已通过。连续开关使用已恢复焦点的Enter，避免Playwright自动滚动因卡片hover抬起造成3px测试前置变化。`verify_cards.cjs` 独立控件/拖选/分页也通过；13项及7797一致性测试通过。截图 outputs/demand-check/dialog-*.png。手机为模拟尺寸，不代表真机触摸验证。尚未录制新主站版本或验证本次下载落盘。

### 本轮交付收口（2026-09-11）

以上“未提交”等描述为各阶段记录；本轮将用途发现、SEO资料、卡片与弹窗交互、间距修复和两份浏览器回归脚本一并提交。用途区到卡片间距桌面24px、手机16px；1280px下悬浮后仍保留21px，390px触屏模拟无横向溢出。静态构建已通过。

主站本地3016已通过用户Chrome实际JSON下载：彩铅日记/三步知识讲解/植物发芽，文件提示词与预览142字符逐字一致。功能范围弹窗打开锁背景、关闭恢复焦点已实测；完整卡片与长详情验证使用隔离Chrome。真实录屏因系统录屏启动失败未完成，真机触摸未验证；代码推送不代表主站部署。验收记录及截图位于忽略目录 outputs/main-site-acceptance/。

### 后续本地按钮优化（96ff34a之后，未提交）

关闭按钮为44×44的SVG叉号，圆形点击区域、悬浮浅灰底，键盘focus-visible保留单层高对比轮廓。去掉独立方框详情按钮；标题为原生button并常显箭头，支持Tab/Enter/Space，关闭后焦点回标题；用途区末尾明确提示点击卡片查看详情。图片/空白点击、独立控件排除、文字拖选、24/16px留白和既有弹窗滚动锁保持。1280/390卡片、分页、长弹窗回归通过，另实测Space、焦点恢复、44px按钮与24/16间距。手机仍为模拟视口。预览为 outputs/main-site-acceptance/title-*.png、close-*.png。本批不提交推送部署。

### 详情布局与焦点收口（后续本地反馈，未提交）

关闭圆圈差异来自此前打开dialog后立即focus关闭按钮，浏览器按输入方式决定focus-visible。现在打开聚焦tabindex=-1的详情标题（dialog仍通过aria-labelledby命名），关闭常态浅灰圆底无描边，Tab到关闭仍有高对比轮廓；无全局outline关闭。header使用44px/弹性标题/44px三列对称布局，长标题换行；固定footer中按钮桌面280px、手机满宽、高至少48px。

verify_detail_header.cjs 验证1280/390鼠标打开、Enter打开、Tab焦点、循环、关闭恢复、标题和按钮中心、长标题布局（明确为测试fixture）。verify_dialog.cjs与verify_cards.cjs本轮重跑通过。截图 outputs/main-site-acceptance/header-{mouse,keyboard,long}-{1280,390}.png；手机为模拟视口，真实读屏/真机未验证。

### 预览弹窗交互契约（本地实现中）

面向已选定风格、正在修改主题的创作者：表单提交成功后就地打开独立preview dialog，不隐藏表单、不调用step(3)的聚焦和滚到顶部。标题/摘要、提示词、可选独立标题在可滚动正文内，固定头部关闭和底部复制/下载；仅导出提示词，不自动生图。关闭、Escape恢复原表单值、位置与提交按钮焦点，并清除旧pack；再次提交从当前输入重新渲染。顶部仅预览打开期间标为03，关闭恢复02。

配色冲突使用已有表单内提示，不开预览、不叠dialog；取消本次预览保留输入。记住与沿用下一主题移至表单侧栏，能力保留。共享原生dialog锁背景/恢复逻辑，静态打开不添加入场位移动画，减少动态不影响流程；移动端近全屏。验收覆盖两配色分支、取消、重复编辑、键盘/长文、复制JSON一致、记住与下一主题，以及1280/390视口。

本轮结果：13项Python测试与7797组跨运行时一致性通过；verify_preview.cjs覆盖1280/390的取消配色提示、风格/主题配色两分支、重复编辑、Enter/Tab/Escape、原位置/输入/焦点恢复、长提示词固定操作栏、实际剪贴板与JSON下载内容一致、记住及下一主题；无非GET/API请求或pageerror。共享弹窗调整后verify_cards、verify_dialog、verify_detail_header全部复验通过。静态构建通过，手机截图已检查。非交互标题作为读屏焦点不显示操作轮廓，交互按钮focus-visible保留。主站同步、用户视觉验收、真机/真实读屏和录屏未完成，本轮不提交推送部署。

### 复制与下载 Toast（本地待验收）

官方资料评估：Ant Design为React库（https://ant.design/docs/react/introduce/）；Toastify-js为MIT原生库，支持selector挂载、时长和ariaLive（https://github.com/apvarun/toastify-js）。本项目只有一个预览提示位，之前仅有announce文本状态，并无Toast helper；现在新增小型原生helper，无额外依赖/CDN，不复制第三方源码。

Toast挂在preview dialog内部header，同属top layer；预留固定头部高度，居中覆盖标题视觉位置但保留可访问名称，不遮关闭或正文、不出现时推挤。白底轻阴影+状态图标+文案。复制await实际成功才提示可粘贴到生图工具；拒绝则选中提示词，持续提示手动系统复制并可关闭。下载仅说已发起请确认保存。成功3秒、单实例更新、关闭清理，epoch与pack身份防止旧异步复制在新预览冒出；不抢焦点，关闭错误Toast恢复触发按钮；礼貌live region，减少动态时关闭淡入动画，无主题进入提示文案。

verify_toast.cjs在1280/390验证真实剪贴板和JSON、明确模拟复制拒绝与延迟、单实例、3秒消失/失败持续、键盘关闭、长文滚动、固定布局、reduced-motion、关闭重开清理；全部通过。截图outputs/main-site-acceptance/toast-success-*.png、toast-error-*.png。仍需主站同步后实际3016验证；新Toast用户视觉、真机/读屏未验收，无提交推送部署。

### Toast视觉反馈修订（用户未通过后的本地修复）

取消顶部白色Toast及隐藏标题做法，标题始终显示。固定底部操作区设置64px反馈位，默认显示复制使用说明，并非空白；深炭色Toast在该位置覆盖说明，主文案16px半粗、次文13px、图标辅助，不遮正文或按钮、不改变位置。复制成功按钮同步显示“已复制 ✓”，保留固定aria-label避免重复播报，4秒恢复；失败持续可关闭，下载仍只表示已发起。保留epoch异步清理/减少动态/焦点。

verify_toast与verify_preview均在源侧1280/390通过；新增断言Toast位于正文下方且在按钮上方、前后按钮矩形一致、标题opacity=1、4秒按钮文字恢复。截图toast-success/error-1280/390.png已更新，手机成功状态目视核对通过。新包待主站同步及用户视觉验收，未提交推送部署。

### Toast面积精简（用户否决深色横幅后，本地待确认）

去掉满宽深色背景和第二行说明，改内容自适应宽、44px高的浅薄荷单行成功浮条，绿色勾和深绿16px字；操作区预留位从64缩至44px，不再常驻说明。标题与按钮位置保持，复制按钮短暂状态不添加边框。失败持续可关闭，已选中提示词可使用系统复制；下载简写“已发起下载，请确认保存”。1280/390源侧Toast与预览回归通过，桌面手机截图已目视检查，仅确认无大横幅/遮挡，不代表用户视觉通过。截图toast-compact-success/error-1280/390.png；包待主站查看后同步。无提交推送部署。

### 本批发布交接收口（2026-09-11）

用户已认可compact视觉并授权本批提交推送。最终采用单行浅薄荷小浮条，此前白色顶部与深色横幅仅为过程记录，不进入当前行为。主站实际 http://127.0.0.1:3016/koi-stylekit/gallery/ 已运行最新verify_toast与verify_preview，1280/390全部通过；真实复制与下载JSON一致、主题修改后使用新值；拒绝/延迟为测试fixture，手机为模拟视口。完整详情/卡片回归为同批此前3016结果。主站任务负责其固定制品部署与线上验收；本仓库推送不代表主站已上线。真机、真实读屏与录屏仍未验证。

### 详情图片加载稳定性（本地候选，2026-09-11）

- 复现：冷缓存延迟返回纸雕详情图，1280px 视口关闭按钮上移 71.23px，正文下移 213.39px。
- 修复：目录保存原图实际 `image_width` / `image_height`；静态构建读取编码后的详情 WebP 实际尺寸（避免缩放取整误差）。详情图提前设置宽高，保持 `contain` 和 420px 高度上限。失败文案覆盖原图片区域，不收缩布局。未改变图片内容。
- 新增/替换源图片时同步其宽高元信息；目录测试核对全部 308 张 PNG 的真实宽高，构建自动记录 WebP 宽高。
- 验证：`node scripts/verify_image_layout.cjs`（需 Playwright/Chrome，默认本地 4347/dist，可用 `GALLERY_URL` 覆盖）用隔离上下文延迟/中止详情图片请求。1280×800、390×800 × 横/竖/方 × 成功/失败，共 12 个场景；加载前后 header、关闭、图片、正文、footer 的位置与尺寸差值均为 0，关闭/背景锁/焦点恢复通过。
- `python3 -m unittest discover -s tests -q`：13 项通过，7,797 组跨运行时输出一致。构建后全部 308 张详情图元信息与文件尺寸一致；955 个静态文件中仅 `index.html`、`styles.json` 与已发布制品不同，所有图片字节相同。
- 截图与几何：`outputs/image-layout-before/`、`outputs/image-layout-after/`。已查看桌面竖图与手机失败占位，无裁切或多余的大块空白。未验证真机/Safari/读屏；未提交、推送或部署，待主站同步本地验收。

### 详情信息层级（本地候选，2026-09-11）

详情帮助访客先判断画风和适用用途，再了解限制并选择使用。桌面详情最大宽度 880px，左样图右信息；700px 以下单栏，图片高度按可用视口限制。继续按真实尺寸预留、contain 完整展示。相同的摘要与视觉构成只显示一次；用途标签、注意/验证、来源/家族分层，保留全部事实与样图输入链接。header/footer 固定，正文独立滚动且有底部留白。

点击非交互区域可使原生 dialog 获得外壳焦点。仅对外壳自身 `focus` 且 `:focus-visible` 的情况转回标题；不监听冒泡 focusin，不移除全局 outline。鼠标焦点保持原生行为，避免破坏选字。关闭控件的键盘焦点提示保留。

- `scripts/verify_detail_layout.cjs`：1280×800、1512×824、390×800、800×500及临时隔离 Chrome 设置页真实 125%/200% 页面缩放，常规/长标题/长来源共14场景通过。未使用 CSS zoom；记录 DPR 与 innerWidth 验证缩放生效。末项滚入后底部留白 23.9–28.4px，控件可见，标题/CTA 居中，文字选择正常，全部事实保留。
- `scripts/verify_image_layout.cjs`：12个慢加载/失败场景重新通过，所有加载前后几何差值仍为0；正文测量目标改为新结构的 `.detail-summary`。
- `scripts/verify_dialog.cjs`：保留重复开关、使用风格、焦点/滚动恢复及减少动画检查；紧凑布局没有溢出时不再要求正文产生滚动，有溢出时仍要求可滚动。
- 截图/报告：`outputs/detail-layout/` 与 `outputs/detail-layout-load-final/`。主站任务已目视桌面/手机初稿；这不是用户最终视觉确认。当前仅本地候选，未提交、推送或部署。

### 2026-09-12 配色提醒直接操作（本地候选）

默认 ask 命中颜色词后显示两种配色按钮，用户明确点选后继续预览；提醒滚动至吸顶区域下并取得焦点。保留取消编辑及原有渲染规则。`verify_preview.cjs` 在普通静态服务下通过 1280/390px 提醒可见性、两条直接预览路径、复制与 JSON 下载、焦点恢复；手机为模拟视口。未提交、推送或部署，交接固定包：`koi-color-choices-20260912-075909.tar.gz`。

### 2026-09-12 点击遮罩关闭（本地候选）

详情与提示词原生弹窗共用外侧点击关闭。仅主指针在弹窗外侧按下并在同处点击才关闭；内部点击、内部拖到外部、外部拖到内部、右键不关闭。沿用关闭恢复和 Esc/X。`verify_backdrop.cjs` 在 1280/390px 验证两弹窗、内部点击/拖动、外侧点击/模拟触摸、反复开关、焦点和页面滚动/表单保留；`verify_preview.cjs` 配色/复制/JSON 回归通过。移动端为模拟 Chrome，未做真机 Safari。仅本地，未提交推送部署。固定包：`koi-backdrop-20260912-203041.tar.gz`。

### 2026-09-12 小屏布局（本地候选）

移动筛选从三行收为搜索+范围/用途/筛选两行；卡片压缩段落与操作间距，保留字号和完整图片；详情图片上限降低，正文与固定底部仍分开滚动。短屏横屏取消页面吸顶，宽横屏卡片两列。保留上一轮未提交的遮罩关闭。`verify_mobile_layout.cjs` 检查320/375/390/430竖屏、667×375及844×390横屏、1280桌面，涵盖无横向溢出、筛选展开收起、详情最后字段/关闭按钮可达及键盘焦点。`verify_backdrop.cjs`与`verify_preview.cjs`回归通过。固定包：`koi-mobile-layout-20260912-204600.tar.gz`；未提交推送部署。

### 2026-09-12 详情展示区对齐（本地候选）

用途标签移至图片下居中；描述及说明字段仍左对齐，列表卡片不改。verify_mobile_layout增加标签位置与对齐检查，七种尺寸通过，截图见outputs/detail-alignment。固定包：`koi-detail-alignment-20260912-211441.tar.gz`。包含未提交的遮罩关闭和小屏布局；未提交推送部署。
